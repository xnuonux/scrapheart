{
  "name": "scrapheart",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "P0. one zone, one companion. the gate is whether people name it.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "seedrandom": "^3.0.5",
    "zzfx": "^1.3.2"
  },
  "devDependencies": {
    "@types/seedrandom": "^3.0.8",
    "typescript": "^5.6.3",
    "vite": "^6.0.5"
  }
}
